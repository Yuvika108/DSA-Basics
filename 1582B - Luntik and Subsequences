#include <iostream>
#include <vector>
#include <cmath>

using namespace std;

void solve() {
    int n;
    cin >> n;
    
    long long count_zeros = 0;
    long long count_ones = 0;
    
    for (int i = 0; i < n; ++i) {
        int a;
        cin >> a;
        if (a == 0) count_zeros++;
        else if (a == 1) count_ones++;
    }
    
    // The number of nearly full subsequences is (count of 1s) * 2^(count of 0s)
    // We use 1LL << count_zeros to calculate 2^count_zeros efficiently
    long long result = count_ones * (1LL << count_zeros);
    
    cout << result << endl;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}
